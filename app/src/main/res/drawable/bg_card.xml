<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Fondo  -->
    <solid android:color="@color/card_bg_light"/>

    <!-- Borde -->
    <stroke
        android:width="1dp"
        android:color="@color/background_input"/>

    <!-- Esquinas redondeadas -->
    <corners android:radius="10dp"/>

    <!-- (Opcional) padding interno de la tarjeta -->
    <padding
        android:left="8dp"
        android:top="8dp"
        android:right="8dp"
        android:bottom="8dp"/>
</shape>
