<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Fondo clarito -->
    <solid android:color="@color/purpleVeryLight"/> <!-- un lila muy claro -->

    <!-- Bordes redondeados -->
    <corners android:radius="12dp" />

    <!-- Línea/borde -->
    <stroke
        android:width="1dp"
        android:color="@color/bg_input" />
</shape>